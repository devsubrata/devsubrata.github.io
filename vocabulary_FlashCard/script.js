
/*
Array.from(document.querySelectorAll(".front")).forEach((item) => {

    item.onclick = () => {

        item.classList.toggle("change");
    };

});

Array.from(document.querySelectorAll(".back")).forEach((item) => {

    item.onclick = () => {

        item.classList.toggle("change");
    };

});
*/